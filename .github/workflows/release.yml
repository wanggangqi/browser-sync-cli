name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: windows
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: macos-intel
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: macos-arm

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install dependencies
        run: pnpm install

      - name: Build Extension
        run: pnpm build:extension

      - name: Verify Extension Build
        shell: bash
        run: |
          echo "Extension dist contents:"
          ls -la packages/extension/dist/
          echo "manifest.json:"
          cat packages/extension/dist/manifest.json

      - name: Build Native Host (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          cd packages/native-host
          cargo build --release --target ${{ matrix.target }}
          mkdir -p release
          cp target/${{ matrix.target }}/release/browser-sync-native-host.exe release/
          cp install.ps1 release/

      - name: Build Native Host (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          cd packages/native-host
          cargo build --release --target ${{ matrix.target }}
          mkdir -p release
          cp target/${{ matrix.target }}/release/browser-sync-native-host release/
          cp install.sh release/ || true

      - name: Build Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.event.inputs.version || github.ref_name }}
          releaseName: 'Browser Sync CLI ${{ github.event.inputs.version || github.ref_name }}'
          releaseBody: 'See assets below.'
          releaseDraft: true
          prerelease: false
          args: --target ${{ matrix.target }}

      - name: Package Extension (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          cd packages/extension
          7z a -tzip ../../extension.zip dist

      - name: Package Extension (macOS)
        if: matrix.os == 'macos-latest' && matrix.target == 'x86_64-apple-darwin'
        run: |
          cd packages/extension
          zip -r ../../extension.zip dist

      - name: Upload Extension
        if: matrix.os == 'windows-latest' || (matrix.os == 'macos-latest' && matrix.target == 'x86_64-apple-darwin')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: extension.zip
          draft: true

  create-release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create tag if not exists
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a ${{ inputs.version }} -m "Release ${{ inputs.version }}" || echo "Tag already exists"
          git push origin ${{ inputs.version }} || echo "Tag already pushed"